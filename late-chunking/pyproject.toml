[project]
name = "late-chunking"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "numpy>=2.2.6",
    "sentence-transformers>=5.1.0",
    "torch>=2.8.0",
    "transformers>=4.55.4",
]
